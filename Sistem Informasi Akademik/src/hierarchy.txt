controller.Main Page
"You're in Campus Portal now"

	Title: register/login

	1.Login
	    1. Mahasiswa
	    2. Staff
	2.Exit


		1. login
		    1. Mahasiswa
		        *display
		            NIM:
		            Nama:
		            Alamat:
		            TTL:
		            Telepon:
		            Jurusan:
		                // after user clicks 'ok' displays their data
		                Attribut for Sarjana, Attribut for Magister, Attribut for Doktor
		    2. Staff
		            NIK:
		            Nama:
                    Alamat;
                    TTL:
                    Telepon:
                         // after user clicks 'ok' displays their data
  		                Attribut for Dosen Tetap, Attribut for Dosen Honorer, Attribut for Karyawan

		3. exit

for login option, we have created data dummy, or demo data dummy that user can simply log in

for register user create new data and linked it to demo data dummy
// it could be one array list object or something


/src
|
|---Library
	----Book



/src
|----User(String nama, String alamat, ttl(String tempat, Date birthdate), telepon) // abstract class
    |
    |---Mahasiswa(nim, jurusan) // abstract class
        |---Sarjana(ArrayList<MataKuliah>)
        |---Master(ArrayList<MataKuliah>, String judulTesis)
        |---Doktor(String judulDisertasi, int sidang1, sidang2, sidang3)
    |
    |---Staff(nik) // abstract class
        |---Dosen(String departmen, ArrayList<MatakulAjar) // abstract class
            |---DosenTetap(double salary)
            |---DosenHonorer(double honorPerSKS)
        |---Karyawan(double salary, List<PresensiStaff>

    (supplement classes) // would not be primary object
    |---Matakuliah(String kode, int sks, String nama)
        |---MatakulPilihan(int minimum)
        |---MatkulAmbil(List<Presensi>, int n1, int n2, int n3)
        |---MatkulAjar(List<PresensiStaff>

    |---Presensi(Date present, int status*from interface*
        |---Presensi Staff(Time(hours)

    |---Interface Status(HADIR = 1, ALPHA = 0)

// MatkulAjar = MataKuliahAjar
// MatkulAmbil = MataKuliahAmbil